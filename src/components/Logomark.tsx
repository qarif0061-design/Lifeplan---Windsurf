/** The Lifeplans app icon. Same square mark used across nav, sidebar, and auth. */
const Logomark = ({ className = "" }: { className?: string }) => (
  <img src="/favicon-192.png" alt="Lifeplans" className={className} />
);

export default Logomark;
